import { SafeAreaView, Text } from "react-native";

export default function TabTwoScreen() {
  return (
    <SafeAreaView className="flex-1 justify-center items-center w-full h-full">
      <Text className="text-2xl">Analyze screen</Text>
    </SafeAreaView>
  );
}
